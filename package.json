{
  "name": "selector-set",
  "version": "1.1.2",
  "description": "An efficient data structure for matching and querying elements against a large set of CSS selectors.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "http://github.com/josh/selector-set.git"
  },
  "main": "./dist/selector-set.js",
  "module": "selector-set.next.js",
  "jsnext:main": "selector-set.next.js",
  "devDependencies": {
    "benchmark": "^1.0.0",
    "chrome-remote-interface": "^0.26.1",
    "d3": "^3.5.10",
    "eslint": "^5.5.0",
    "eslint-plugin-github": "^1.2.2",
    "qunitjs": "^1.20.0",
    "rollup": "^0.65.2"
  },
  "scripts": {
    "build": "rollup -c && cp selector-set.next.js.flow dist/selector-set.js.flow",
    "pretest": "npm run build && eslint --report-unused-disable-directives .",
    "test": "./script/test",
    "prepublish": "npm run build"
  },
  "files": [
    "dist/selector-set.js.flow",
    "dist/selector-set.js",
    "selector-set.next.js.flow",
    "selector-set.next.js"
  ]
}
